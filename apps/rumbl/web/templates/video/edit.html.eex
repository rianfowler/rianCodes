<h2>Edit videos</h2>

<%= render "form.html", changeset: @changeset,
                        categories: @categories,
                        action: video_path(@conn, :update, @videos) %>

<%= link "Back", to: video_path(@conn, :index) %>
